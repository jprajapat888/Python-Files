# Suppose, we have a loop or a function that is not implemented yet, but we want to implement it in the future. It cannot have an empty body, otherwise, the interpreter would complain.

sequence = {'p', 'a,' 's', 's'}
for val in sequence:
    pass

print("Statement after loop")
